import Icon from './Icon';

export type Row = { id: number; titulo: string; descricao: string; status: string; data: string; cor?: string };
export default function AdminTable({ rows }: { rows: Row[] }) {
  return <div className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-6 py-4">Conteúdo</th><th className="px-6 py-4">Status</th><th className="px-6 py-4">Data</th><th className="px-6 py-4 text-right">Ações</th></tr></thead><tbody>{rows.map(row => <tr className="border-b border-slate-100 last:border-0" key={row.id}><td className="px-6 py-4"><div className="flex items-center gap-4"><span className={`h-12 w-16 rounded-lg bg-gradient-to-br ${row.cor || 'from-blue-200 to-emerald-200'}`} /><span><strong className="block">{row.titulo}</strong><small className="mt-1 block max-w-md truncate text-slate-500">{row.descricao}</small></span></div></td><td className="px-6 py-4"><span className={`status ${row.status === 'Publicada' || row.status === 'Aprovada' ? 'bg-emerald-50 text-emerald-700' : row.status === 'Pendente' ? 'bg-amber-50 text-amber-700' : 'bg-slate-100 text-slate-600'}`}>{row.status}</span></td><td className="px-6 py-4 text-slate-500">{row.data}</td><td className="px-6 py-4"><div className="flex justify-end gap-2">{(['edit', 'eye', 'trash'] as const).map(icon => <button key={icon} className={`grid h-9 w-9 place-items-center rounded-lg border ${icon === 'trash' ? 'text-rose-500' : 'text-slate-500'} hover:bg-slate-50`}><Icon name={icon} size={16} /></button>)}</div></td></tr>)}</tbody></table></div></div>;
}
